from django.urls import path
from product import views
urlpatterns = [
    # path('', views.ProductViewSet.as_view(),name='product-lists'),
    # path('<int:id>/', views.ViewSpecificProduct.as_view(), name='product-list')
]
